akka {
  loglevel = INFO
}

spray.can.server {
  request-timeout = 1s
}

workingDir = "working"

prodDB = {
  path = ${workingDir}"/PDFExtractionToolkit.db"
  url = "jdbc:sqlite:"${prodDB.path}
  driver = org.sqlite.JDBC
  connectionPool = disabled
  keepAliveConnection = true
}

testDB = {
  path = ${workingDir}"/TestPDFExtractionToolkit.db"
  url = "jdbc:sqlite:"${testDB.path}
  driver = org.sqlite.JDBC
  connectionPool = disabled
  keepAliveConnection = true
}